Infographic architecture diagram of the "asearch" CLI search tool for LLM agents. Style: clean, minimal, dark gray (#1a1a2e) background, accent colors: emerald green (#10b981) and white. No shadows, no gradients, flat design like technical documentation.

Center block: "asearch" logo, prominent, emerald green. Four zones around it, connected by arrows:

Left: "LLM Agent" block with robot icon, labels: "Claude Code", "Codex", "Cursor", "OpenCode". Arrow right to asearch: "JSON".

Top — "Sources" zone, split into three columns:
1. "Zero-config" — checkmark icon, labels: "hn (Algolia API)", "reddit (cookies)", "github (gh CLI)", "jina (Reader)", "youtube (cookies)"
2. "API keys" — key icon, labels: "Tavily", "Exa (neural)", "Brave (35B index)"
3. "CLI tools" — terminal icon, labels: "twitter-cli → X"

Right — "Workflow" zone, three steps connected by arrows:
1. "open" — search icon, label: "asearch open --query \"...\" --source web,hn,reddit"
2. "results" — list icon, labels: "read (paginated)", "filter (by source)", "--raw (piping)"
3. "session" — close icon, labels: "list", "close", "gc"

Bottom: "JSON Contract" block with curly braces icon and two code snippets:
'{"ok":true,"sid":"a1b2c3d4","total":42,"next_commands":{...}}' — "metadata-first"
'{"seq":1,"limit":20,"results":{"items":[...]}}' — "paginated reads"

Bottom small text: "asearch open → results read → results filter → session close"

16:9 aspect ratio, no natural language except technical terms shown.
